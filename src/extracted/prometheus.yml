global:
  scrape_interval: 15s
  evaluation_interval: 15s

rules_files:
  - /etc/prometheus/prometheus_rules.yml

scrape_configs:
  - job_name: 'orb-analyzer'
    static_configs:
      - targets: ['analyzer:8002']

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
